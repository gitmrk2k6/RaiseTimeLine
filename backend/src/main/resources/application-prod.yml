spring:
  datasource:
    url: "jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME:raisetimeline}"
    username: "${DB_USERNAME}"
    password: "${DB_PASSWORD}"

logging:
  level:
    com.raisetimeline: INFO
    root: WARN

management:
  endpoint:
    health:
      show-details: always
